name: CICD - Main
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  call-workflow-in-another-repo:
    uses: raghudevopsb05/roboshop-github-actions/.github/workflows/ci-main.yml@main


